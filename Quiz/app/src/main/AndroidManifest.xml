<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hr.math.quiz">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".RegisterActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".MyScoresActivity" />
        <activity android:name=".AllScoresActivity" />
        <activity android:name=".CategoryActivity" />
        <activity android:name=".game.activities.QuestionSelectActivity" />
        <activity android:name=".game.activities.QuestionInputActivity" />
        <activity android:name=".game.activities.GameFinishedActivity" />
        <activity android:name=".game.activities.QuestionDropdownActivity"></activity>
    </application>

</manifest>